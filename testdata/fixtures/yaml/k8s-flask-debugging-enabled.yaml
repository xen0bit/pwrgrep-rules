apiVersion: v1
kind: Pod
metadata:
  name: bad1
spec:
  containers:
    - name: app
      image: flask-app
      # ruleid: flask-debugging-enabled
      env: [{name: FLASK_ENV, value: "development"}]
---
apiVersion: v1
kind: Pod
metadata:
  name: bad2
spec:
  containers:
    - name: app
      image: flask-app
      # ruleid: flask-debugging-enabled
      env: [{name: FLASK_ENV, value: "development"}]
---
apiVersion: v1
kind: Pod
metadata:
  name: good1
spec:
  containers:
    - name: app
      image: flask-app
      # ok: flask-debugging-enabled
      env: [{name: FLASK_ENV, value: "production"}]
---
apiVersion: v1
kind: Pod
metadata:
  name: good2
spec:
  containers:
    - name: app
      image: flask-app
      # ok: flask-debugging-enabled
      env: [{name: OTHER, value: "development"}]
