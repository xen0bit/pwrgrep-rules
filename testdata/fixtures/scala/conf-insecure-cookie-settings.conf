# Session cookies without the Secure flag travel over cleartext HTTP.
# Leave secure enabled (or omit it) instead of forcing false.
play.http.session.secure = true
session = {
  // ruleid: conf-insecure-cookie-settings
  secure = false
}
session = {
  // ruleid: conf-insecure-cookie-settings
  secure = false
}
session = {
  // ruleid: conf-insecure-cookie-settings
  secure = false
}
# ok: conf-insecure-cookie-settings
play.http.session.secure = true
# ok: conf-insecure-cookie-settings
play.http.session.maxAge = 3600
# ok: conf-insecure-cookie-settings
play.http.secret.key = "fixed-secret-key"
