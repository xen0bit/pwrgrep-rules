# rules: detected-gitlab-token
# fixture: generic/detected-gitlab-token.txt
# from: CWE-798 (Use of Hard-coded Credentials)

. as $root
| ( ($root | scan_regex(["*"]; [
      "glpat-(?P<SECRET>[A-Za-z0-9_\\-]{20,})"
    ])) as $all
    | ( $all | of(["glpat-(?P<SECRET>[A-Za-z0-9_\\-]{20,})"])
          | where_capture_entropy("SECRET")
        | finding("detected-gitlab-token";
        "this is a gitlab token written into the source, so it is in every checkout "
        + "and every image built from one; read it from the environment, and "
        + "rotate this one because it has leaked") ) )
| report
